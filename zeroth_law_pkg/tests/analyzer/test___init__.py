"""Tests for src/zeroth_law/analyzer/__init__.py."""

import pytest


def test_analyzer_init_exists():
    """Ensures the test file has at least one test."""
    # This __init__.py is mostly empty, so not much to test directly.
    # Coverage for imported modules happens via their own tests.
    assert True
