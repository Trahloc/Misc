# Pre-commit config for hugsearch
# FILE_LOCATION: Placeholder - this project might have its own template or be managed differently.
repos:
  []
# Removed all hooks as hugsearch is currently not a ZLF project
# and its pre-commit execution was failing due to dependency issues.
# This allows commits to proceed while hugsearch-specific pre-commit
# strategy and dependency conflicts are addressed.
